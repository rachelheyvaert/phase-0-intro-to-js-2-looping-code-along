const messages = [];
const names = ["Guadalupe", "Ollie", "Aki"];
const event = "surprise";

function writeCards(names){
    for (let n = 0; n < names.length; i++ ){
        console.log(`Thank you, ${names[n]}, for the wonderful birthday gift!`);
    } return names
}
